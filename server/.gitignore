# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS files
.DS_Store
# OS files
.DS_Store
Thumbs.db

# Environment files
# Environment files
.env
.env.local
.env.*.backup
.env.backup
.auth.json

# Editor configs
# Editor configs
/.vscode
/.idea
/.fleet
/.zed
/.phpstorm.meta.php

# PHP / Laravel
# PHP / Laravel
.phpactor.json
.phpunit.result.cache
/.phpunit.cache
/vendor
composer.phar
/storage/*.key
/storage/pail
/storage/framework/cache
/storage/framework/sessions
/storage/framework/views

# Public build (Laravel + Vite)
# Public build (Laravel + Vite)
/public/build
/public/hot
/public/storage

# Frontend / Node
# Frontend / Node
/node_modules
.vite
.cache

# Monorepo frontend
# Monorepo frontend
/clients/dist
/clients/build

# Homestead
# Homestead
Homestead.json
Homestead.yaml

# Notes:
# - Keep `composer.lock` and frontend lockfiles (package-lock.json / pnpm-lock.yaml / yarn.lock)
#   committed for deterministic installs. Do NOT add them to this file.